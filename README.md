# sztcmh-mri-kidney-metrics

Kidney MRI metric tooling for Shenzhen Traditional Chinese Medicine Hospital workflows.

## Versioning

- `v0-code-0`: preserved raw import of the historical project
- `v1` target: behavior-preserving architecture refactor
- `v2` target: product and UX modernization

## Running

```bash
python main.py
```

## Tests

```bash
python -m unittest discover -s tests/unit -v
```
